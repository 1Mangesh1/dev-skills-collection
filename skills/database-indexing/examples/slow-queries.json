{
  "slow_query_examples": [
    {
      "query_id": 1,
      "sql": "SELECT * FROM orders o JOIN customers c ON o.customer_id = c.id WHERE o.created_at > '2024-01-01'",
      "execution_time_ms": 2500,
      "rows_examined": 500000,
      "rows_returned": 1200,
      "problems": [
        "No index on created_at",
        "Fetching all columns (*)",
        "Missing index on foreign key"
      ],
      "suggested_indexes": [
        "CREATE INDEX idx_orders_created ON orders(created_at)",
        "CREATE INDEX idx_orders_customer ON orders(customer_id)"
      ],
      "optimized_query": "SELECT o.id, o.total, c.name FROM orders o JOIN customers c ON o.customer_id = c.id WHERE o.created_at > '2024-01-01'",
      "after_optimization_ms": 150
    }
  ],
  "metrics": {
    "total_slow_queries_found": 12,
    "potential_speedup": "10-100x",
    "index_count_needed": 8,
    "storage_overhead_mb": 150
  }
}
